// Fibonacci
function fibo(n) {
    // Caso(s) base
    if(n === 0) {
        return 0
    }
    // Llamada recursiva
    return ??? + fibo(n-1)

}


// Primera llamada
let result = fibo(5) // 8
console.log(result)